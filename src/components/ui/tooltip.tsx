// DEAD FILE — This component is not used anywhere in the project.
// Safe to delete. Was previously used via TooltipProvider in providers.tsx
// which has been removed as React Query / Tooltip were never consumed.
export {};
